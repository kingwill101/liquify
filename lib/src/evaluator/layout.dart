// These are no longer needed as we build the hierarchy at once
// _LayoutInfo? _extractLayoutInfo(List<ASTNode> nodes) { ... }
// Future<_LayoutInfo?> _extractLayoutInfoAsync(List<ASTNode> nodes) async { ... }
// }
